/*
  validate_configuration.sql
  This application has no database-stored configuration table (see
  install/009_seed_configuration.sql — configuration is entirely
  environment-variable driven). Nothing to validate here — kept as a
  placeholder for structural consistency with the standard RAH Lab database
  package layout.
*/
PRINT 'N/A — this project has no database-stored configuration table (see install/009_seed_configuration.sql).';
GO
