/*
  005_create_views.sql
  No SQL views exist in this application. node-backend/server.js performs all
  reporting (dashboard stats, wastage, response-times) via raw SQL SELECTs
  with inline subqueries/UNIONs rather than database views — confirmed by
  inspecting every query in server.js.

  This file is kept as a placeholder so the install sequence numbering stays
  stable if views are introduced later.
*/
