/*
  validate_lookup_data.sql
  This application has no normalized lookup tables (see
  install/008_seed_lookup_data.sql for why). Nothing to validate here — kept
  as a placeholder for structural consistency with the standard RAH Lab
  database package layout.
*/
PRINT 'N/A — this project has no normalized lookup tables (see install/008_seed_lookup_data.sql).';
GO
